#include <iostream>
#include <string>
using namespace std;

int n;
string s[21];
char start;

string is_connected(string a, string b);
int main()
{
    cin >> n;
    
    for (int i = 0; i < n; i++)
        cin >> s[i];
    cin >> start;
}

string is_connected(string a, string b)
{
    for(int i = 0; i < a.size() && i < b.size(); i++)
    {
        
    }
}